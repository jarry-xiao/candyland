/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link TreeAuthority}
 * @category Accounts
 * @category generated
 */
export type TreeAuthorityArgs = {
  creator: web3.PublicKey
  delegate: web3.PublicKey
  totalMintCapacity: beet.bignum
  numMintsApproved: beet.bignum
  numMinted: beet.bignum
}

export const treeAuthorityDiscriminator = [244, 193, 241, 144, 28, 102, 125, 64]
/**
 * Holds the data for the {@link TreeAuthority} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class TreeAuthority implements TreeAuthorityArgs {
  private constructor(
    readonly creator: web3.PublicKey,
    readonly delegate: web3.PublicKey,
    readonly totalMintCapacity: beet.bignum,
    readonly numMintsApproved: beet.bignum,
    readonly numMinted: beet.bignum
  ) {}

  /**
   * Creates a {@link TreeAuthority} instance from the provided args.
   */
  static fromArgs(args: TreeAuthorityArgs) {
    return new TreeAuthority(
      args.creator,
      args.delegate,
      args.totalMintCapacity,
      args.numMintsApproved,
      args.numMinted
    )
  }

  /**
   * Deserializes the {@link TreeAuthority} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [TreeAuthority, number] {
    return TreeAuthority.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link TreeAuthority} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey
  ): Promise<TreeAuthority> {
    const accountInfo = await connection.getAccountInfo(address)
    if (accountInfo == null) {
      throw new Error(`Unable to find TreeAuthority account at ${address}`)
    }
    return TreeAuthority.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, treeAuthorityBeet)
  }

  /**
   * Deserializes the {@link TreeAuthority} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [TreeAuthority, number] {
    return treeAuthorityBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link TreeAuthority} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return treeAuthorityBeet.serialize({
      accountDiscriminator: treeAuthorityDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link TreeAuthority}
   */
  static get byteSize() {
    return treeAuthorityBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link TreeAuthority} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      TreeAuthority.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link TreeAuthority} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === TreeAuthority.byteSize
  }

  /**
   * Returns a readable version of {@link TreeAuthority} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      creator: this.creator.toBase58(),
      delegate: this.delegate.toBase58(),
      totalMintCapacity: (() => {
        const x = <{ toNumber: () => number }>this.totalMintCapacity
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      numMintsApproved: (() => {
        const x = <{ toNumber: () => number }>this.numMintsApproved
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      numMinted: (() => {
        const x = <{ toNumber: () => number }>this.numMinted
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const treeAuthorityBeet = new beet.BeetStruct<
  TreeAuthority,
  TreeAuthorityArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['creator', beetSolana.publicKey],
    ['delegate', beetSolana.publicKey],
    ['totalMintCapacity', beet.u64],
    ['numMintsApproved', beet.u64],
    ['numMinted', beet.u64],
  ],
  TreeAuthority.fromArgs,
  'TreeAuthority'
)
